
{
  "rules": {
    ".read": "auth != null",
    ".write": "auth != null",
    "products": {
      ".indexOn": ["createdAt"]
    },
    "settings": {
      "modalTarget": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    }
  }
}
